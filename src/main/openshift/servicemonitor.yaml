apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: swim-dnotam-provider-validator-metrics
  labels:
    app: swim-dnotam-provider-validator
    app.kubernetes.io/name: swim-dnotam-provider-validator
    app.kubernetes.io/part-of: swim-operator
spec:
  selector:
    matchLabels:
      app: swim-dnotam-provider-validator
  endpoints:
  - port: http
    path: /q/metrics
    interval: 30s
    scrapeTimeout: 10s
